PORTAL_HOST_PORT=8111
PORTAL_HTTP_ADDR=:8111

# Sibling tool links on the portal home page (Compose host ports).
# Leave empty in Kubernetes — ci-dash / review / scanner are Compose-only (no K8s manifests).
PORTAL_CI_DASH_URL=http://localhost:8115
PORTAL_REVIEW_URL=http://localhost:8118
PORTAL_SCANNER_URL=
